<!-- <nav>
  <div class="title">
    <span>Restaurant</span>
  </div>
  <div class="right-side-items">
    <div class="right-side-text">
    <% if(!user){ %>
      <button method='GET' action='/login/:id'><a href="/login/1">Login1</a></button>
      <button method='GET' action='/login/:id'><a href="/login/2">Login2</a></button>
    <%} else if(user.id === 1) { %>
      <button method='GET' action='/'><a href="/">Logout</a></button>
      <button method='GET' action='/register'>Orders</button>
    <% } else { %>
      <button method='GET' action='/'><a href="/">Logout</a></button>
      <button method='GET' action='/register'>My Cart</button>
    <% } %>
    </div>
    <div class="double-down-arrow">
      <i class="fa-solid fa-angles-down" ></i>
    </div>
  </div>
</nav> -->

<nav class="navbar sticky-top navbar-expand-lg navbar-dark bg-dark">
  <div class="container-fluid">
    <a class="navbar-brand" href="/">Restaurant</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <!-- <ul class="navbar-nav"> -->
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="/">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Features</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Pricing</a>
        </li>
        <li class="nav-item">
          <a class="nav-link disabled">Disabled</a>
        </li>
        <!-- <li class="nav-item d-flex"> -->
      </ul>
      <div class="d-flex">
        <% if(!user){ %>
          <div>
            <form action='/login/' method='POST'>
              <input type="text" placeholder="user" name="user">
              <input type="text" placeholder="psw" name="psw">
              <button class="me-2">Login</button>
            </form>
          </div>
        <%} else if(user.id === 1) { %>
          <label style="margin-right: 10px" for="logout">Logged in as <%= user.name %>  </label>
          <form action='/'  method='GET' >
            <button class="me-2">Logout</button>
          </form>
          <form action='/orders' >
            <button>Orders</button>
          </form>
        <% } else { %>
          <label style="margin-right: 10px" for="logout">Logged in as <%= user.name %>  </label>
          <form action='/' method='GET'>
            <button class="me-2">Logout</button>
          </form>
          <from action='/myCart'>
            <button>My Cart</button>
          </from>
        <% } %>
      </div>
    </div>
  </div>
</nav>


